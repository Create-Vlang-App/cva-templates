# Plotting guide

Use `build_demo_scatter()` as a starting point. For the full chart gallery, see
upstream `vlang/vsl` examples (`plot_*`) linked from `docs/recipes/DOMAIN_RECIPES.md`
in cva-templates.

Avoid `plt.show()` in unit tests/CI.
