# Docker guide

Multi-stage Dockerfile and `docker-compose.yml` for V apps.

```bash
docker compose up --build
```

Default publish port is `8080` (web-server). Adjust `EXPOSE` / compose ports for CLI or systems binaries.
