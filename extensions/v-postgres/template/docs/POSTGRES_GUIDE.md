# PostgreSQL guide

Adds `docker-compose.postgres.yml` and `.env.example` for a local Postgres service.

```bash
docker compose -f docker-compose.postgres.yml up -d
```

Copy `.env.example` and wire connection settings into your V app.
