# Configuration

| Flag | Short | Default | Description |
|------|-------|---------|-------------|
| `--name` | `-n` | `world` | Name to greet |
| `--help` | `-h` | — | Show help |

Extend `Command` / `Flag` definitions in `main.v`. Pure string logic belongs in feature modules such as `greet`.
