# cli-app

CLI starter using V `cli` + `os`, with a `greet/` feature module.

## Run

```bash
v run . -- --name world
```

## Test

```bash
v test .
```

## Docs

See [docs/README.md](docs/README.md). Quality bar: [QUALITY.md](QUALITY.md).
