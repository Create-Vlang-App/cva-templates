# Agent notes

Library scaffold for Create Vlang App. Public API lives under `src/`; examples under `examples/`.

## Verify

```bash
v test .
v run examples/example.v
```

Before publishing, align `v.mod` `name` with the `module` declaration in `src/`.
