# Contributing

1. Keep changes focused; match existing module style.
2. Add or update `*_test.v` next to production code.
3. Run `v fmt -w .`, `v vet .`, and `v test .` before opening a PR.
4. Update `docs/` when structure, config, or deploy steps change.
