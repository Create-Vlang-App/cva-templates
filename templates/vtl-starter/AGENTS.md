# Agent notes

Tensor workflows via [vtl](https://github.com/vlang/vtl).

Prefer top-level feature modules (`import tensor` → `tensor/`). Copy `_module_template/` for new features.

## Verify

```bash
v test .
v run .
```
