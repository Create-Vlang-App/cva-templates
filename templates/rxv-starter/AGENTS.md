# Agent notes

Reactive pipelines via [rxv](https://github.com/ulises-jeremias/rxv).

Prefer top-level feature modules (`import pipeline` → `pipeline/`). Copy `_module_template/` for new features.

## Verify

```bash
v test .
v run .
```
